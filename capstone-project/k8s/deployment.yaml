apiVersion: apps/v1
kind: Deployment
metadata:
  name: blood-classifier
  labels:
    app: blood-classifier
spec:
  replicas: 2
  selector:
    matchLabels:
      app: blood-classifier
  template:
    metadata:
      labels:
        app: blood-classifier
    spec:
      containers:
      - name: blood-classifier
        image: cancer-predictor:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 5